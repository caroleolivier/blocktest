import React, { Component } from 'react';
import { BlockGridContainer } from './blockGridContainer';

export class App extends Component {
    render() {
        return (
            <BlockGridContainer />
        )
    }
}